# BodyScan KB & Memory — dependências
# Instalar com: pip install -r requirements.txt

# Framework
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# Banco de dados (MVP: SQLite async)
sqlalchemy>=2.0.30
aiosqlite>=0.20.0
alembic>=1.13.1

# Pydantic v2 + settings
pydantic>=2.7.0
pydantic-settings>=2.2.1

# Embeddings (local, sem API key — provider padrão do MVP)
sentence-transformers>=3.0.0

# Embeddings via OpenAI (opcional — configurado por env var)
openai>=1.30.0

# Utilitários
python-dotenv>=1.0.1
httpx>=0.27.0

# Testes
pytest>=8.2.0
pytest-asyncio>=0.23.6
pytest-cov>=5.0.0
httpx>=0.27.0  # TestClient async

# Qualidade de código
ruff>=0.4.4
mypy>=1.10.0
